target-location = "127.0.0.1:{TOML_FOLDER}/target/{TODAY}"

rsync-options = [ "--numeric-ids", "--checksum" ]

exclude = [ "cache", ".cache", "__pycache__" ]

[backup-folders]
"{TOML_FOLDER}/data-to-backup" = { target = "data-to-backup", exclude = [ "cache" ] }
