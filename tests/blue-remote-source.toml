target-location = "{TOML_FOLDER}/target/{TODAY}"

exclude = [ "cache", ".cache", "__pycache__" ]

[backup-folders]
"127.0.0.1:{TOML_FOLDER}/data-to-backup" = { target = "data-to-backup", exclude = [ "cache" ] }
